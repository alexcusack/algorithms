// Write a function that produces all the (x, y) coordinates in an NxM grid

// Input: N and M dimensions of a grid
// Output: List of coordinates on the grid

var getCoordinates = function(n,m){
  var possibleCoordinates = []
  var count = 0
  var coodinate
  while (coodinate[0] <= n && coodinate[1] <= m){
    coodinate=[]
  }


  return possibleCoordinates
}


// console.log(getCoordinates(4,4))
console.log(getCoordinates(2,2))